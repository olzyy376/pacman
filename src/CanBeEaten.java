/**
 * An interface that shows the common characteristics between cherries, dot, pellet and frenzy ghosts
 * as all of them are eatable
 */
public interface CanBeEaten{
    /**
     * method that is used to get the score of each eatable objects
     */
    int getScore();
}
